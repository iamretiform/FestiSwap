# you can write to stdout for debugging purposes, e.g.
# puts "this is a debug message"
#A=[..N-1]
#A[0]=the first node aka highest level aka parent
#A[K]=A[K]
#if A[last_node]=-1
# then last_node = A.length - 1
#else A[successor_]


class returns_the_length_of_the_array
  attr_accessor :a, :A
  def initialize
    a = A.length
  end

  def self.solution(a)
    return a 
  end
  # write your code in Ruby 2.2
end



A[0] =  1
A[1] =  4
A[2] = -1
A[3] =  3
A[4] =  2